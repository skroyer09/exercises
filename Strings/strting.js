"use strict";

const longline =
  "A very, very, very, very, VERY! long line of text,\
 that spans arcoss the with of the editor!";

const name = "Peter";
const drink = "Pepsi";

console.log(`Hello ${name}, would you like a ${drink}?`);

const petName = "Mandu";
const petType = "Cat";

console.log(`My name is ${name}.
I have a ${petType} called ${petName}`);

// it also be done this way, with the link \n but you don't have to use it.
//console.log(`My name is ${name}.\nI have a ${petType} called ${petName}`);
